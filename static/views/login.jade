form(method='post', id='signin_form', class='login')
    .info
        h2 Welcome back to twaddlr!
        p Have no account yet? <a href="#" class="register-link">Register</a> now!

    .fields
        .row.input
            label Login
            input#login(type='text', name='login')
        .row.input
            label Password
            input#password(type='password', name='password')

        .row.buttons
            button(type='submit') Login
